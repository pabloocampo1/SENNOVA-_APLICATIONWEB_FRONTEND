import { Box } from '@mui/material';
import React from 'react';

const ProfilePage = () => {
    return (
        <Box>
            En construccion
        </Box>
    );
};

export default ProfilePage;